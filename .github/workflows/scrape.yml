name: Scrape Depth Charts

on:
  schedule:
    - cron: '0 10 * * 2'  # Every Tuesday at 10am UTC
  workflow_dispatch:        # Allows manual trigger from GitHub UI

jobs:
  scrape:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 nflreadpy pandas rapidfuzz
      
      - name: Run depth chart scraper
        run: python scripts/scrape_depth.py

      - name: Run OTC contract scraper
        run: python scripts/scrape_otc.py

      - name: Run stats scraper
        run: python scripts/scrape_stats.py

      - name: Run Madden merge
        run: python scripts/merge_madden.py

      - name: Build player master
        run: python scripts/build_master.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/
          git diff --staged --quiet || git commit -m "auto-scrape depth charts"
          git push